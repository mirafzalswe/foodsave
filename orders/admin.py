from django.contrib import admin

# Note: The orders app currently has no models defined.
# This admin.py file is ready for future model registrations.
# If you add models to orders/models.py, register them here following
# the same patterns used in other apps.
